% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/det_32.R
\name{det_32}
\alias{det_32}
\title{Apply adaptive detrending to an rwl object}
\usage{
det_32(
  rwl_object,
  threshold_long = 100,
  threshold_medium = 50,
  threshold_short = 30,
  spline_nyrs_long = 32,
  spline_nyrs_medium = 20,
  spline_nyrs_short = 10,
  spline_nyrs_shortest = 5,
  try_modnegexp_short = TRUE,
  try_modnegexp_shortest = TRUE
)
}
\arguments{
\item{rwl_object}{An object of class 'rwl' (e.g., as read by dplR::read.rwl).}

\item{threshold_long}{Length threshold to use spline_nyrs_long (default: 100).}

\item{threshold_medium}{Length threshold to use spline_nyrs_medium (default: 50).}

\item{threshold_short}{Length threshold to use spline_nyrs_short (default: 30).}

\item{spline_nyrs_long}{Years for the spline in long series (default: 32).}

\item{spline_nyrs_medium}{Years for the spline in medium series (default: 20).}

\item{spline_nyrs_short}{Years for the spline in short series (default: 10).}

\item{spline_nyrs_shortest}{Years for the spline in very short series if ModNegExp fails (default: 5).}

\item{try_modnegexp_short}{Attempt ModNegExp for short series before spline_nyrs_short (default: TRUE).}

\item{try_modnegexp_shortest}{Attempt ModNegExp for very short series before spline_nyrs_shortest (default: TRUE).}
}
\value{
A data.frame where each column is a detrended series (Ring Width Index - RWI),
and row names correspond to the years.
}
\description{
This function applies different detrending methods (Spline or ModNegExp)
based on the length of each individual series within an rwl object, using
preset thresholds and spline stiffness common in certain dendrochronological
workflows (e.g., targeting a 32-year spline for longer series).
}
\examples{
if (requireNamespace("dplR", quietly = TRUE)) {
  # Load example data from dplR
  data(co021, package = "dplR")
  # Apply adaptive detrending with default parameters
  rwi_co021 <- det_32(co021)
  # View the first few rows/columns
  print(head(rwi_co021[, 1:5]))

  # Example with modified parameters
  # rwi_co021_alt <- det_32(co021, threshold_long = 150, spline_nyrs_long = 50)
}

}
